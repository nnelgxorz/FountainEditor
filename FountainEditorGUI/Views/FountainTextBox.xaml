<UserControl x:Class="FountainEditorGUI.Views.FountainTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:e="clr-namespace:FountainEditorGUI.Extensions"
             xmlns:vm="clr-namespace:FountainEditorGUI.ViewModels"
             mc:Ignorable="d"
               DataContext="{e:InjectInstance {x:Type vm:FountainTextBoxViewModel}}"
             d:DataContext="{d:DesignInstance {x:Type vm:FountainTextBoxViewModel}}"
             d:DesignHeight="300" d:DesignWidth="300" Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Style TargetType="{x:Type Paragraph}">
            <Setter Property="Margin" Value="150,0,100,0"/>
        </Style>
    </UserControl.Resources>

    <RichTextBox Name="DisplayBox" AcceptsReturn="True" AcceptsTab="True" FontFamily="Courier Prime" FontSize="16" ScrollViewer.VerticalScrollBarVisibility="Auto" BorderBrush="Black" />
</UserControl>
